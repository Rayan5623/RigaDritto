// Copia lato client di server/sieveRules.js (stesso pattern di zoneRules.js:
// vedi quel file per la spiegazione del perché duplichiamo). Il server resta
// l'unica fonte di verità.

export const SIEVE_BUCKETS = ["ORA", "DOPO", "POI", "ATTESA", "PARCHEGGIO"];
